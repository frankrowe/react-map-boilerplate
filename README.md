React Map Boilerplate
==============

A boilerplate project to create a map with Leaflet, React, and Browserify.

First, run `npm install`

To build, run `npm run build`

To watch, run `npm run watch`
